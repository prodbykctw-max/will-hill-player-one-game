// Audio stub. No sound implementation yet — placeholder module so the
// src/ structure matches CLAUDE.md's documented layout.

export function createAudio() {
  return {
    play() {},
    stop() {},
  };
}
